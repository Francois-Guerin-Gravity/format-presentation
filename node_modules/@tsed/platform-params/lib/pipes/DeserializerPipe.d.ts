import { ParamMetadata, PipeMethods } from "../domain/ParamMetadata";
export declare class DeserializerPipe implements PipeMethods {
    private settings;
    transform(value: any, param: ParamMetadata): any;
}
